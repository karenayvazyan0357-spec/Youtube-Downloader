# YouTube Downloader

Веб-приложение на Flask для скачивания видео и аудио с YouTube. Поддерживает выбор качества, скачивание только аудио и автоматическое объединение видео/аудио потоков через ffmpeg.

## 🚀 Возможности

- 📥 Скачивание видео в различных качествах (от 144p до максимального)
- 🎵 Скачивание только аудио
- 🎬 Автоматическое объединение видео и аудио потоков для высокого качества
- 📊 Отображение информации о видео (название, автор, длительность, просмотры)
- ⚡ Асинхронная загрузка с отслеживанием статуса
- 🎨 Современный веб-интерфейс

## 📋 Требования

- Python 3.7+
- [ffmpeg](https://ffmpeg.org/download.html) (для объединения видео и аудио потоков)

## 🔧 Установка

1. **Клонируй репозиторий:**
   ```bash
   git clone https://github.com/karenayvazyan0357-spec/youtube-downloader.git
   cd youtube-downloader
   ```

2. **Создай виртуальное окружение:**
   ```bash
   python3 -m venv .venv
   source .venv/bin/activate  # На macOS/Linux
   # или
   .venv\Scripts\activate  # На Windows
   ```

3. **Установи зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Установи ffmpeg:**
   
   **macOS:**
   ```bash
   brew install ffmpeg
   ```
   
   **Linux (Ubuntu/Debian):**
   ```bash
   sudo apt update
   sudo apt install ffmpeg
   ```
   
   **Windows:**
   - Скачай с [официального сайта](https://ffmpeg.org/download.html)
   - Добавь в PATH

## 🎯 Использование

1. **Запусти приложение:**
   ```bash
   python app.py
   ```

2. **Открой браузер и перейди по адресу:**
   ```
   http://localhost:5001
   ```

3. **Вставь ссылку на YouTube видео** и выбери качество

4. **Дождись завершения загрузки** и скачай файл

## 📁 Структура проекта

```
youtube-downloader/
├── app.py              # Основной файл приложения
├── requirements.txt    # Зависимости Python
├── templates/          # HTML шаблоны
│   └── index.html
├── static/             # Статические файлы
│   ├── css/
│   │   └── style.css
│   └── js/
│       └── script.js
└── downloads/          # Папка для загруженных файлов (создается автоматически)
```

## 🛠 Технологии

- **Flask** - веб-фреймворк
- **pytubefix** - библиотека для работы с YouTube
- **ffmpeg** - объединение видео и аудио потоков

## ⚙️ Конфигурация

В файле `app.py` можно изменить:

- `DOWNLOAD_FOLDER` - папка для сохранения файлов
- `MAX_CONTENT_LENGTH` - максимальный размер файла
- `SECRET_KEY` - секретный ключ Flask (для продакшена используй переменные окружения)
- `port` - порт приложения (по умолчанию 5001)

## 📝 Примечания

- Приложение работает в режиме отладки (`debug=True`)
- Для продакшена рекомендуется использовать переменные окружения для конфигурации
- Загруженные файлы сохраняются в папке `downloads/`

## 🤝 Вклад

Приветствуются Pull Request'ы! Если нашел баг или есть идея для улучшения - создай Issue.

## 📄 Лицензия

Этот проект распространяется свободно. Используй на свой страх и риск.

## ⚠️ Отказ от ответственности

Этот инструмент предназначен только для личного использования. Убедись, что у тебя есть права на скачивание контента в соответствии с условиями использования YouTube и авторским правом.
